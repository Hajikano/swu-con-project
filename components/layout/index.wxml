<view hidden="{{hidden}}" style="background-color:{{bgColor}};">
    <view wx:if="{{isNavBar}}" style="{{DC_Layout.style.space}}"></view>
    <view style="{{DC_Layout.style._}}height:{{isNavBar?DC_Layout.navBar:DC_Layout.un_navBar}}px">
        <slot />
    </view>
</view>